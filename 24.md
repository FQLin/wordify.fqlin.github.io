选择**方案二（在本地从源码编译 `canvas`）**是一条稍微硬核的路径。由于 `canvas` 并不是纯 JavaScript 代码，它的底层依赖了大量的 C++ 图形库（如 Cairo、Pango 等）。在 Node.js 24 没有预编译包的情况下，你必须在自己的 Windows 电脑上把这些 C++ 源码编译成可执行文件。

整个过程大约需要下载几 GB 的工具，请按照以下步骤依次进行：

### 第一步：准备 C++ 与 Python 编译环境

这是 `node-gyp`（Node.js 的原生模块编译工具）运作的基础。

1. **全局安装 node-gyp**
以**管理员身份**打开 PowerShell 或 CMD，运行：
```bash
npm install -g node-gyp

```


2. **安装 Python 3**
去官网下载并安装最新的 Python 3。**核心注意：** 在安装界面的底部，务必勾选 **“Add Python to PATH”**（将 Python 添加到环境变量）。
3. **安装 Visual Studio Build Tools**
* 下载 [Visual Studio Build Tools 2022](https://www.google.com/search?q=https://visualstudio.microsoft.com/zh-hans/downloads/%23build-tools-for-visual-studio-2022)（不需要安装完整的 VS 开发工具，只要 Build Tools 即可）。
* 打开安装程序，在“工作负荷 (Workloads)”中，勾选 **“使用 C++ 的桌面开发” (Desktop development with C++)**。
* 点击右下角的安装（此步骤大约需要下载和占用 6GB+ 的空间，请耐心等待）。



### 第二步：安装 GTK 2 (核心图形渲染依赖)

`canvas` 强依赖 GTK 2 提供的底层图形能力。**注意：必须是 GTK 2，不能是 GTK 3 或 4。**

1. 下载 64 位的 GTK 2 压缩包：[点击这里下载 gtk+-bundle_2.22.1-20101229_win64.zip](http://ftp.gnome.org/pub/gnome/binaries/win64/gtk+/2.22/gtk+-bundle_2.22.1-20101229_win64.zip) (这是 node-canvas 官方推荐的构建包)。
2. 将压缩包解压。**强制要求：必须解压到 `C:\GTK` 目录**。
*(检查一下：解压后，你的电脑里应该存在 `C:\GTK\bin` 和 `C:\GTK\lib` 这两个文件夹。`node-gyp` 脚本写死了会去这个 C 盘固定路径找文件。)*

### 第三步：安装 libjpeg-turbo (JPEG 图片支持)

如果你的代码中需要处理 `.jpeg` / `.jpg` 图片，还需要安装这个库。

1. 下载 [libjpeg-turbo 的 Windows 64位安装包](https://sourceforge.net/projects/libjpeg-turbo/files/) (寻找带有 `vc64.exe` 后缀的文件)。
2. 运行安装程序，**强制要求：必须安装到默认路径 `C:\libjpeg-turbo64**`。

### 第四步：清理项目并重新触发编译

以上环境全部就绪后，回到你的项目目录：`C:\Work Space\Enterprise\Gitlib\UI Layer\mf-react-super-user-management-china`。

1. 彻底清理旧的失败残留和缓存：
```bash
rm -rf node_modules
rm yarn.lock
yarn cache clean

```


2. 重新执行安装命令：
```bash
yarn install

```


*此时，由于找不到预编译包，`node-pre-gyp` 会自动调用 `node-gyp`，并利用你刚安装的 VS C++ 工具和 `C:\GTK` 中的文件开始漫长的本地编译。如果控制台出现大量黄色的 C++ 编译警告，那是正常现象，只要最后不报红色的 `ERR!` 即可。*

---

> **💡 避坑指南：** > 即使编译成功，在启动项目时，如果终端提示 `Error: %1 is not a valid Win32 application` 或 `找不到指定模块`，请将 `C:\GTK\bin` 目录下的所有 `.dll` 文件，手动复制到你项目的 `node_modules/canvas/build/Release/` 目录下。

这个过程涉及的环境比较多，容易在某个环节卡壳。你希望先去安装第一步的 C++ 编译工具，还是在此之前想考虑和团队商量一下直接把 Node.js 降级到 v20（方案一）？

完全不需要！既然你已经安装了 VS2026，那太好了，你可以直接使用它。这能帮你省去重新下载几 GB 文件的漫长等待。

`node-gyp` 的核心需求只是微软的 C++ 编译器（MSVC）和 Windows SDK，只要是较新的 Visual Studio 版本都可以胜任。

基于你现有的 VS2026，你只需要做以下两点微调，就可以无缝衔接编译过程：

### 1. 确认已安装 C++ 工作负荷

请打开你的 **Visual Studio Installer**，找到你已安装的 VS2026，点击“修改”。
确保 **“使用 C++ 的桌面开发” (Desktop development with C++)** 这一项是**勾选状态**。如果已经勾选，直接关闭即可；如果没有，请勾选并让它完成增量安装。

### 2. 手动指定 node-gyp 使用的版本

`node-gyp` 的自动检测机制有时候比较“笨”，可能会默认去寻找旧版本（比如 2019 或 2022）的路径。为了防止它在编译时“迷路”报错，我们需要明确告诉 npm 和 yarn 使用你电脑上的版本。

请在终端（建议管理员权限）中运行以下命令：

```bash
npm config set msvs_version 2026
yarn config set msvs_version 2026

```

---

完成这两步后，你本地的 C++ 编译环境就已经彻底准备就绪了（即替代了上一条回复中的“第一步”）。

你现在可以直接跳到前面的 **第二步（下载并解压 GTK 2 到 `C:\GTK`）** 继续往下操作。

需要我现在为你提供 GTK 2 和 libjpeg-turbo 的直接下载链接，方便你快速搞定剩下的环境吗？

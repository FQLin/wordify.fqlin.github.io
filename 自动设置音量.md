没问题，我们采用**日历订阅方案**。这个方案最稳定，不需要联网请求 API，而且你可以直观地在日历里看到哪天是“班”，哪天是“休”。

以下是保姆级的操作步骤。

### 第一步：订阅“包含补班信息”的日历

我们需要一个专门标注了“休”和“班”字的日历源，普通的中国节假日日历往往只标节日，不标补班。

**推荐订阅源（开源社区维护，稳定更新）：**
这是一个 GitHub 上非常热门的项目（shuyz/china-holiday-cal），专门用于自动化。

1. **复制下面的链接**：
```text
https://www.shuyz.com/github/files/cal/holidays-calendar-subscribe.ics

```


2. 打开 iPhone 上的 **“日历” (Calendar)** App。
3. 点击底部的 **“日历”** 按钮（中间位置）。
4. 点击左下角的 **“添加日历”** -> 选择 **“添加订阅日历”**。
5. 在弹出的框中粘贴刚才复制的链接，点击 **“订阅”**。
6. **关键设置**：
* 在之后的设置页面，给这个日历起个好认的名字，比如 **“法定节假日”**。
* **颜色**：建议选个显眼的红色。
* 点击右上角 **“添加”**。


7. **验证**：回到日历视图，翻到最近的一个法定节假日（比如五一或十一），看看是否有带有“休”或“班”字的日程出现。如果有，第一步成功。

---

### 第二步：编写快捷指令（核心逻辑）

这一步我们要教手机：**“优先看日历，如果日历没说话，再看今天是星期几”。**

1. 打开 **“快捷指令”** App，点击右上角 **+** 新建。
2. **添加操作 1：获取日历日程**
* 搜索并添加 **“查找日历日程”**。
* 点击“所有日程”，设置过滤条件：
* **开始日期** 是 **今天**。
* **日历** 是 **法定节假日**（就是你刚才订阅的那个）。


* *注意：这一步是确保我们只读取节假日信息，不读取你的私人会议。*


3. **添加操作 2：判断逻辑（处理特殊情况）**
* 搜索并添加 **“如果”**。
* 点击“输入”，选择 **“日历日程”**。
* 点击日历日程，在弹出的属性里选择 **“标题”**。
* 条件改为 **“包含”**，文本框输入：**班**。
* **结果**：如果是“班”，说明今天是调休补班（哪怕是周日也要上班）。
* **在“如果”下面添加操作**：**“设置音量”** -> 调整为 **工作音量（如 50%）**。
* **重要步骤**：搜索并添加 **“退出快捷指令”**，放在“设置音量”下面。
*(意思：既然确定是班了，调完音量就收工，不用往下判断了)*


4. **添加操作 3：判断逻辑（处理放假情况）**
* 在刚才的“否则”下面，再添加一个 **“如果”**。
* 同样判断 **“日历日程”** 的 **“标题”**。
* 条件改为 **“包含”**，文本框输入：**休**。
* **结果**：如果是“休”，说明今天是周三也没关系，反正是法定假。
* **在“如果”下面添加操作**：**“设置音量”** -> 调整为 **休息音量（如 0%）**。
* 同样添加 **“退出快捷指令”**。


5. **添加操作 4：判断逻辑（处理普通日子）**
* 如果上面两个“如果”都没跑通，说明今天既不是特殊的“班”，也不是特殊的“休”，就是个普通日子。
* 搜索并添加 **“获取日期”**（获取当前日期）。
* 搜索并添加 **“格式化日期”**。
* 点击“日期格式”，选择 **“自定”**。
* 格式字符串输入小写字母：`e`
* *解释：`e` 代表星期的数字，周日是1，周一到周六是2-7。*


* 搜索并添加 **“如果”**。
* 判断“格式化后的日期”。
* 条件选择 **“是”**，输入 **1** （这是周日）。


* 点击“如果”左边的图标，选择 **“添加其他条件”**（或者再套一个如果），把 **7** （这是周六）也加进去。
* *简单写法*：直接用 **“匹配文本”** 正则表达式 `[17]` 匹配格式化后的结果。
* *通俗写法*：如果结果是 1，设置休息音量；否则如果结果是 7，设置休息音量。


* **最终逻辑**：
* 如果是 1 或 7（周末） -> **设置休息音量**。
* 否则（是 2,3,4,5,6） -> **设置工作音量**。




6. **起名保存**：给指令起名，例如 **“每日音量检查”**。

---

### 第三步：设置自动化（每天自动跑）

1. 进入 **“自动化”** 标签页 -> **新建自动化**。
2. 选择 **“特定时间”**。
3. 设定时间：**07:30**（在你闹钟响之前，或者出门前）。
4. 重复：**每天**（必须是每天，因为周六周日也可能要补班，需要检查）。
5. 勾选 **“立即运行”**，关闭“通知”。
6. 下一步，选择 **“运行快捷指令”**，选中刚才写的 **“每日音量检查”**。

---

### 逻辑复盘（为什么这样设置最稳？）

假设今天是 **2026年2月15日（周日）**，但是因为春节调休需要 **补班**。

1. **自动化启动**。
2. **获取日程**：订阅日历显示今天有个全天日程叫 “春节补班”。
3. **判断1**：标题包含“班”吗？ -> **包含**。
4. **执行**：设置工作音量 -> **退出**。
5. **结果**：虽然是周日，但音量正确开启。

假设今天是 **某一个周三**，但是是 **国庆节**。

1. **获取日程**：订阅日历显示 “国庆节休”。
2. **判断1**：包含“班”吗？ -> 不包含。
3. **判断2**：包含“休”吗？ -> **包含**。
4. **执行**：设置休息音量 -> **退出**。
5. **结果**：虽然是周三，但音量正确关闭。

这样你就拥有了一个完美的、不需要联网也能识别中国调休政策的音量控制器了。
